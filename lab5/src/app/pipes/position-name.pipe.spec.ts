import { PositionNamePipe } from './position-name.pipe';

describe('PositionNamePipe', () => {
  it('create an instance', () => {
    const pipe = new PositionNamePipe();
    expect(pipe).toBeTruthy();
  });
});
